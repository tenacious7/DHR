# TODO: fix the dynamic header left elements that are not visible in other section of the worker dashbord.it only showing for dashboard/worker not for others

## Changes Reverted
- Reverted app-sidebar.tsx to use original icons (Home, CreditCard, Wallet, Phone)
- Reverted dynamic-header.tsx to use Cross instead of Ambulance
- Reverted conditional-layout.tsx to exclude worker dashboard from sidebar layout
- Reverted worker dashboard page to include its own sidebar components



